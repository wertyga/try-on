export type SendHelpMessageReq = {
  message: string;
};

export type EnvMap = {
  stripePk: string;
  minCloseDistance: number;
  cacheTime: number;
  cacheDropIdentifier: string;
  runtimeVersion: string;
};
